# Capacitor Plugin Registry

## Getting Started

The goal of this project is to build out a single location where users can find plugins that are built for Capacitor. It is built using pnpm, NextJS, MUI Joy, and Supabase.

## Contributing

Clone this repo and install dependencies using `pnpm i`. Afterwords, simply run `pnpm dev` to start the dev server.
